services:
  carla-voices:
    container_name: carla-voices
    image: usdotfhwastoldev/carla-voices:pilot2-event0-latest
    command: /bin/bash ./CarlaUE4.sh -vulkan -nosound #-quality-level=Low
    privileged: true
    network_mode: "host"
    environment:
      - DISPLAY=$DISPLAY
      - SDL_VIDEODRIVER=x11
      - XAUTHORITY=~/.Xauthority
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ${HOME}/voices-poc:/home/voices-poc
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  voices:
    container_name: voices
    image: usdotfhwastoldev/voices:base-latest
    tty: true 
    depends_on:
      - carla-voices
    network_mode: "host"
    environment:
      - DISPLAY=$DISPLAY
      - SDL_VIDEODRIVER=x11
      - XAUTHORITY=~/.Xauthority
      - VUG_SITE_CONFIG_FILE=andrew_docker.config
      - VUG_DOCKER_START_EM=true
      - VUG_DOCKER_START_CONSOLE=true
      - VUG_DOCKER_START_CANARY=true
      - VUG_DOCKER_START_TDCS=false
      - VUG_DOCKER_START_DATAVIEW=false
      - VUG_DOCKER_START_MANUAL_CARLA_VEHICLE=true
      - VUG_DOCKER_MANUAL_CARLA_VEHICLE_IS_NEW=true # DO NOT CHANGE - BASE MUST SPAWN NEW MANUAL VEHICLE
      - VUG_DOCKER_START_SUMO=false
      - VUG_DOCKER_SUMO_CONFIG=mcity_sumo/mcity_map_v2.sumocfg
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ${HOME}/voices-poc:/home/voices-poc
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]